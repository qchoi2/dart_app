# Progress

## 2026-07-15

### OpenDART API 키를 `.env`로 분리

- 프로젝트 내 API 키 사용 경로를 MCP 서버, 직접 실행 스크립트, Claude 설정 병합 스크립트에서 확인했다.
- 실제 API 키를 Git에서 제외되는 `.env`로 이동하고, 공유 가능한 `.env.example`을 추가했다.
- 외부 패키지 없이 `.env`를 읽는 공통 로더를 추가하고 모든 실행 경로에 적용했다.
- Claude 설정 파일에 API 키를 복사하던 로직을 제거했다.
- Windows 설치 안내서를 `.env` 사용 방식에 맞게 수정했다.
- Python 문법 검사, `.env` Git 제외, 환경변수 우선순위, OpenDART API 인증을 확인했다.

### 작업 원칙 반영

- 모든 작업에서 `AGENTS.md`를 참고한다.
- 작업 완료 시 이 파일을 갱신한 후 관련 변경사항을 적절한 메시지로 커밋한다.
- 비밀 정보가 포함된 `.env`는 커밋하지 않는다.

### 프로젝트 전반 검토 및 초보자 사용 흐름 정리

- Python 소스 5개, Windows 배치 파일 2개, 설치 안내서와 환경설정을 검토했다.
- 핵심 목적은 Claude Desktop 또는 명령줄에서 OpenDART 공시 목록·본문 키워드·재무정보를 조회하는 것으로 확인했다.
- Python 문법 검사와 키워드 필터·본문 발췌 함수의 오프라인 동작 검사를 통과했다.
- 현재 PC에는 안내된 `mcp` 패키지가 없으며, 배치·설정 병합 스크립트에 고정된 Python 및 Claude 설정 경로도 존재하지 않음을 확인했다.
- Claude 전체 설정을 프로젝트 폴더에 복사하는 보안 위험, 잘못된 JSON을 빈 설정으로 덮어쓸 위험, `.env` 도입 후 문서의 파일 구성 설명이 맞지 않는 문제를 확인했다.
- 회사코드 없는 공시검색은 최대 3개월이라는 OpenDART 공식 제한과 달리 기본 검색기간이 6개월 이상인 문제를 확인했다.
- 오류 누락, 재시도 부재, 영구 캐시, 중복 구현, 재무계정 단순 문자열 매칭, 테스트·의존성 명세 부재를 후속 개선 후보로 정리했다.
- 이번 작업은 진단과 설명만 수행했으며 실제 코드 동작은 변경하지 않았다.
