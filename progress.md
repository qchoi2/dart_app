# Progress

## 2026-07-15

### OpenDART API 키를 `.env`로 분리

- 프로젝트 내 API 키 사용 경로를 MCP 서버, 직접 실행 스크립트, Claude 설정 병합 스크립트에서 확인했다.
- 실제 API 키를 Git에서 제외되는 `.env`로 이동하고, 공유 가능한 `.env.example`을 추가했다.
- 외부 패키지 없이 `.env`를 읽는 공통 로더를 추가하고 모든 실행 경로에 적용했다.
- Claude 설정 파일에 API 키를 복사하던 로직을 제거했다.
- Windows 설치 안내서를 `.env` 사용 방식에 맞게 수정했다.
- Python 문법 검사, `.env` Git 제외, 환경변수 우선순위, OpenDART API 인증을 확인했다.

### 작업 원칙 반영

- 모든 작업에서 `AGENTS.md`를 참고한다.
- 작업 완료 시 이 파일을 갱신한 후 관련 변경사항을 적절한 메시지로 커밋한다.
- 비밀 정보가 포함된 `.env`는 커밋하지 않는다.

### 프로젝트 전반 검토 및 초보자 사용 흐름 정리

- Python 소스 5개, Windows 배치 파일 2개, 설치 안내서와 환경설정을 검토했다.
- 핵심 목적은 Claude Desktop 또는 명령줄에서 OpenDART 공시 목록·본문 키워드·재무정보를 조회하는 것으로 확인했다.
- Python 문법 검사와 키워드 필터·본문 발췌 함수의 오프라인 동작 검사를 통과했다.
- 현재 PC에는 안내된 `mcp` 패키지가 없으며, 배치·설정 병합 스크립트에 고정된 Python 및 Claude 설정 경로도 존재하지 않음을 확인했다.
- Claude 전체 설정을 프로젝트 폴더에 복사하는 보안 위험, 잘못된 JSON을 빈 설정으로 덮어쓸 위험, `.env` 도입 후 문서의 파일 구성 설명이 맞지 않는 문제를 확인했다.
- 회사코드 없는 공시검색은 최대 3개월이라는 OpenDART 공식 제한과 달리 기본 검색기간이 6개월 이상인 문제를 확인했다.
- 오류 누락, 재시도 부재, 영구 캐시, 중복 구현, 재무계정 단순 문자열 매칭, 테스트·의존성 명세 부재를 후속 개선 후보로 정리했다.
- 이번 작업은 진단과 설명만 수행했으며 실제 코드 동작은 변경하지 않았다.

### 프로젝트 전반 개선사항 구현

- 특정 사용자에게 고정된 Python·Claude 설정 경로를 제거하고 현재 Python 및 표준·MSIX Claude 설정을 자동 탐색하도록 변경했다.
- Claude 전체 설정을 프로젝트 폴더로 복사하던 기능을 제거하고, 기존 설정은 원래 위치에 백업한 뒤 원자적으로 저장하도록 변경했다.
- 손상된 JSON이나 잘못된 `mcpServers` 구조는 덮어쓰지 않고 명확한 오류로 중단하도록 했다.
- 회사코드 없는 장기 공시검색을 최대 90일 구간으로 자동 분할하고 날짜·공시유형·회사구분·접수번호 입력 검증을 추가했다.
- 일시적 네트워크·서버 오류 재시도와 API 오류 해석을 추가하고, 본문 검색 결과에 처리·일치·실패 건수와 실패 원인을 포함했다.
- MCP 서버의 중복 공시검색·본문처리 구현을 제거하고 `dart_research.py` 공통 엔진을 사용하도록 통합했다.
- 회사코드 캐시를 7일마다 갱신하고 캐시·검색 결과·설정 사본 등 생성물을 Git에서 제외했다.
- 재무제표 구분과 XBRL 표준 계정ID를 우선해 주요 재무계정을 선택하고, 분기·반기 누적 금액과 선택된 계정ID를 함께 반환하도록 개선했다.
- API 키 명령행 인자를 제거하고 `.env`만 사용하도록 보안을 강화했다.
- 검색 결과를 `output/`에 모으고 디렉터리를 자동 생성하도록 변경했다.
- `README.md`, Windows 초보자 설치 안내서, `requirements.txt`를 추가·정비하고 MCP 도구 5개와 실제 파일 구성을 정확히 설명했다.
- 설정 보호, 기간 분할, 부분 실패, 재시도, 결과 저장, 환경변수, 캐시 갱신, 재무계정 선택을 포함한 자동 테스트 19개를 추가했으며 모두 통과했다.

### 자동 MCP 설치 파일 추가

- `MCP 설치.bat`을 추가해 Python 탐색, `.env` 및 API 키 확인, MCP 패키지 설치, Claude Desktop 등록을 한 번에 수행하도록 했다.
- `.env`가 없으면 예시 파일을 복사하고 메모장을 열어 사용자가 실제 API 키를 안전하게 입력하도록 했다.
- 설치 실패 단계에서 즉시 중단하고 원인을 확인할 수 있도록 오류 처리를 추가했다.
- README와 Windows 설치 안내서의 가장 쉬운 설치 방법을 `MCP 설치.bat` 기준으로 갱신했다.
- 설치 파일에 필수 단계와 고정 사용자 경로가 없는지 확인하는 자동 테스트를 추가했다.
- 전체 자동 테스트 20개와 설치 파일 구조 검사를 통과했다.

### 장기 검색기간의 달력 기준 3개월 분할

- 기존 최대 90일 분할을 사용자가 이해하기 쉬운 달력 기준 3개월 분할로 변경했다.
- `20260101~20260714` 검색은 `1/1~3/31`, `4/1~6/30`, `7/1~7/14`로 나눠 실행하고 결과를 최신순으로 합치도록 했다.
- 월말과 윤년에도 구간이 겹치거나 빠지지 않도록 날짜 보정 로직과 자동 테스트를 추가했다.
- 전체 자동 테스트 21개와 대표 기간 분할 구조 검사를 통과했다.
